---
test:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ansible_playbook_python}}"

  vars:
    test_docker_postgres_image: postgres:16
    test_docker_postgres_password: postgres
    test_docker_postgres_network: mikro-cicd 
